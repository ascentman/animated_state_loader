name: animated_state_loader
description: "Providing an animated customizable widget which displays different state: loading, success, error and info"
version: 1.0.2
homepage: "https://github.com/ascentman/animated_state_loader"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - ui
  - checkmark
  - loader
  - animated
  - widget

funding:
  - https://buymeacoffee.com/ascentman9s

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
