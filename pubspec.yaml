name: animated_state_loader
description:
  "A Flutter package providing an animated loader widget that overlays a child widget.
  The loader supports various animation types such as loading, success, error, and info.
  Customize the appearance, duration, and content of the loader overlay."
version: 0.0.1
homepage: "https://"

topics:
  - ui
  - checkmark
  - loader
  - animated
  - widget

funding:
  - https://buymeacoffee.com/ascentman9s

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
